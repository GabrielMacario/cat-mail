# cat-mail
Python script that connects to Gmail via IMAP, applies dynamic email filters, and saves the results incrementally to CSV and Google Sheets. Designed to run on Google Colab using OAuth, without requiring cloud services or service accounts.
